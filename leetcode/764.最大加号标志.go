package leetcode

/*
 * @lc app=leetcode.cn id=764 lang=golang
 *
 * [764] 最大加号标志
 */

// @lc code=start
func orderOfLargestPlusSign(n int, mines [][]int) int {
	// dpUp, dpDown, dpLeft, dpRight := make([][]int, len(mines)), make([][]int, len(mines)),
	// 	make([][]int, len(mines)), make([][]int, len(mines))

	for i := 0; i < len(mines); i++ {
		for j := 0; j < len(mines[i]); j++ {
			if i == 0 {

			}

		}
	}
	return 1
}

// @lc code=end
