package leetcode

/*
 * @lc app=leetcode.cn id=2250 lang=golang
 *
 * [2250] 统计包含每个点的矩形数目
 */

// @lc code=start
func countRectangles(rectangles [][]int, points [][]int) []int {
	count := make([]int, len(points))
	x, y := make([]uint, 1e9), make([]uint, 100)
	for k := range rectangles {
		for rectangles[k][0] > 0 {

		}
	}
	return count
}

// @lc code=end
